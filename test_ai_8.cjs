async function run() {
  const token = process.env.GEMINI_API_KEY;
  // Try Vertex AI
  const PROJECT_ID = "generativelanguage"; // wait, vertex requires project id
  // I don't know the project id
  // Is it possible that the token is just an access token, but there's a specific way to call the REST endpoint?
}
